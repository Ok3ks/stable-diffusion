# This configuration file was automatically generated by Gitpod.
# Please adjust to your needs (see https://www.gitpod.io/docs/config-gitpod-file)
# and commit this file to your remote git repository to share the goodness with others.

image:
  file: .gitpod.Dockerfile
tasks:
  - before: pip3 install -r requirements.txt
    command: redis-server --daemonize yes && pyenv shell 3.8.13 && echo "Welcome to your Workspace"
vscode:
  extensions:
    - ms-python.python
#condaforge/mambaforge:22.9.0-1
#tasks:
  #before: sudo apt-get install libglib2.0-0 && conda env create -f environment.yaml 
    #inity : conda activate ldm
    
